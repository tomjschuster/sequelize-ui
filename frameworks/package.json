{
  "name": "@sequelize-ui/frameworks",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "nodemon --watch './**/*.ts' --exec 'ts-node' ./debug.ts"
  },
  "dependencies": {
    "@sequelize-ui/core": ""
  },
  "devDependencies": {}
}
