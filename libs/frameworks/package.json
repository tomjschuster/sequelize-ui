{
  "name": "@sequelize-ui/frameworks",
  "description": "",
  "version": "0.0.1",
  "main": "index",
  "types": "index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "nodemon --watch './**/*.ts' --exec 'ts-node -r tsconfig-paths/register' ./debug.ts"
  },
  "dependencies": {
    "@sequelize-ui/core": "*"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
