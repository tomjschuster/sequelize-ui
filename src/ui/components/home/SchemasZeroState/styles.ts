import { classnames } from '@src/ui/styles/classnames'
import { inlineButton } from '@src/ui/styles/utils'

export const text = classnames('text-lg', 'text-center', 'leading-loose')
export const createButton = classnames(inlineButton, 'font-bold', 'hover:bg-green-100')
